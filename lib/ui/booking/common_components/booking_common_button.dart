import 'package:flutter/material.dart';

class BookingCommonButton extends StatelessWidget {
  const BookingCommonButton({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: Text('Booking Common Button'),
    );
  }
}
